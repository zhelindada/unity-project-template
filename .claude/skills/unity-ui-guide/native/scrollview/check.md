# ScrollView — 检查

```
□ Viewport 和 Content 子节点存在
□ Content 有 VerticalLayoutGroup
□ Content 有 ContentSizeFitter(VerticalFit=PreferredSize)
□ Content anchor = (0,1)→(1,1)（不是 0,0→1,1）
□ Viewport 有 Mask 或 RectMask2D
□ ScrollRect.Content 引用正确
□ ScrollRect.Viewport 引用正确
□ ScrollRect.Vertical = true
```
